'use client';
import { useState } from 'react';import { useRouter } from 'next/navigation';import { api } from '../../../../lib/api';
export default function NewAsset(){const router=useRouter();const[error,setError]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const v=Object.fromEntries(new FormData(e.currentTarget));const r=await api('/assets',{method:'POST',body:JSON.stringify({customerId:v.customerId,internalIdentifier:v.internalIdentifier,category:v.category,brand:v.brand||null,model:v.model||null,serialNumber:v.serialNumber||null,imei:v.imei||null})});if(!r.ok)return setError('Não foi possível cadastrar. Verifique o cliente e as permissões.');const a=await r.json();router.push(`/app/assets/${a.id}`);}const field='mt-1 w-full rounded-xl border border-emerald-800 bg-emerald-950 p-3';return <main className="mx-auto max-w-3xl p-8"><h1 className="text-3xl font-bold">Novo equipamento</h1><form onSubmit={submit} className="mt-6 grid gap-4"><label>UUID do cliente<input className={field} name="customerId" required /></label><label>Identificação interna<input className={field} name="internalIdentifier" required /></label><label>Categoria<input className={field} name="category" placeholder="Notebook, impressora…" required /></label><label>Marca<input className={field} name="brand" /></label><label>Modelo<input className={field} name="model" /></label><label>Número de série<input className={field} name="serialNumber" /></label><label>IMEI<input className={field} name="imei" /></label>{error&&<p className="text-red-300">{error}</p>}<button className="rounded-xl bg-emerald-600 px-5 py-3">Cadastrar</button></form></main>}
