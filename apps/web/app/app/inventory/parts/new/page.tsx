'use client'; import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { api } from '../../../../../lib/api';
export default function NewPart(){const router=useRouter(),[form,setForm]=useState({sku:'',description:'',unit:'un',referenceCost:'',referencePrice:''}),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const r=await api('/inventory/parts',{method:'POST',body:JSON.stringify({...form,referenceCost:form.referenceCost||null,referencePrice:form.referencePrice||null})});if(r.ok)router.push(`/app/inventory/parts/${(await r.json()).id}`);else setError((await r.json()).error);}return <main><h1>Nova peça</h1><form onSubmit={submit}><input required placeholder="SKU/código" value={form.sku} onChange={e=>setForm({...form,sku:e.target.value})}/><input required placeholder="Descrição" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><input required placeholder="Unidade" value={form.unit} onChange={e=>setForm({...form,unit:e.target.value})}/><input type="number" min="0" step="0.01" placeholder="Custo de referência" value={form.referenceCost} onChange={e=>setForm({...form,referenceCost:e.target.value})}/><input type="number" min="0" step="0.01" placeholder="Preço de referência" value={form.referencePrice} onChange={e=>setForm({...form,referencePrice:e.target.value})}/><button>Criar</button></form>{error&&<p>{error}</p>}</main>}
