'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { api } from '../../../../lib/api';
export default function NewQuote(){const router=useRouter(),[form,setForm]=useState({customerId:'',customerAssetId:'',title:'',notes:'',validUntil:''}),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const r=await api('/quotes',{method:'POST',body:JSON.stringify({...form,customerAssetId:form.customerAssetId||null,validUntil:form.validUntil||null})});if(r.ok)router.push(`/app/quotes/${(await r.json()).id}`);else setError('Não foi possível criar o orçamento.');}return <main><h1>Novo orçamento</h1><form onSubmit={submit}><input required placeholder="ID do cliente" value={form.customerId} onChange={e=>setForm({...form,customerId:e.target.value})}/><input placeholder="ID do equipamento (opcional)" value={form.customerAssetId} onChange={e=>setForm({...form,customerAssetId:e.target.value})}/><input required placeholder="Título" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><textarea placeholder="Observações" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/><input type="date" value={form.validUntil} onChange={e=>setForm({...form,validUntil:e.target.value})}/><button type="submit">Criar</button></form>{error&&<p>{error}</p>}</main>}
