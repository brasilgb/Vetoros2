/* eslint-disable @typescript-eslint/no-explicit-any */
'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { api } from '../../../../lib/api'; // eslint-disable-line @typescript-eslint/no-explicit-any
export default function NewOrder(){const router=useRouter();const [form,setForm]=useState({customerId:'',assetId:'',title:'',reportedProblem:'',initialNotes:''});const [error,setError]=useState('');async function submit(e:any){e.preventDefault();const r=await api('/service-orders',{method:'POST',body:JSON.stringify({...form,assetId:form.assetId||null})});if(r.ok)router.push('/app/service-orders');else setError('Não foi possível criar a OS.');}return <main><h1>Nova ordem de serviço</h1><form onSubmit={submit}><input required placeholder="ID do cliente" value={form.customerId} onChange={e=>setForm({...form,customerId:e.target.value})}/><input placeholder="ID do equipamento (opcional)" value={form.assetId} onChange={e=>setForm({...form,assetId:e.target.value})}/><input required placeholder="Título" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><textarea required placeholder="Problema relatado" value={form.reportedProblem} onChange={e=>setForm({...form,reportedProblem:e.target.value})}/><textarea placeholder="Observações" value={form.initialNotes} onChange={e=>setForm({...form,initialNotes:e.target.value})}/><button type="submit">Abrir OS</button></form>{error&&<p>{error}</p>}</main>}
