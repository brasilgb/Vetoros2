'use client';import Link from'next/link';import{useEffect,useState}from'react';import{api}from'../../../lib/api';type Supplier={id:string;supplier_number:string;legal_name:string;trade_name:string|null;document_normalized:string|null;primary_contact:string|null;status:string};export default function Suppliers(){const[items,setItems]=useState<Supplier[]>([]),[search,setSearch]=useState(''),[status,setStatus]=useState('');async function load(){const q=new URLSearchParams({...(search?{search}:{}),...(status?{status}:{})});const r=await api(`/suppliers?${q}`);if(r.ok)setItems((await r.json()).items);}useEffect(()=>{void load();},[]);return <main><h1>Fornecedores</h1><input placeholder="Número, nome ou documento" value={search} onChange={e=>setSearch(e.target.value)}/><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">Todos</option><option value="active">Ativos</option><option value="inactive">Inativos</option></select><button onClick={load}>Buscar</button> <Link href="/app/suppliers/new">Novo fornecedor</Link><ul>{items.map(s=><li key={s.id}><Link href={`/app/suppliers/${s.id}`}>#{s.supplier_number} — {s.legal_name}{s.trade_name?` / ${s.trade_name}`:''} — {s.document_normalized||'sem documento'} — {s.primary_contact||'sem contato'} — {s.status}</Link></li>)}</ul></main>}
